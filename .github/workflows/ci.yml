name: CI

# Red de seguridad de Ikigai Agencia (Project360).
#
# En cada push a main y en cada Pull Request revisa que el código compile
# (typecheck), pase el linter y construya (build). Si algo falla, GitHub lo
# marca en rojo — y se ve ANTES de que llegue a producción.
#
# Existe porque hasta ahora todo dependía de que alguien se acordara de correr
# `tsc` y `build` a mano antes de subir.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Si llega un push nuevo, cancela la corrida vieja del mismo branch.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Typecheck · Lint · Build
    runs-on: ubuntu-latest
    steps:
      - name: Traer el código
        uses: actions/checkout@v4

      - name: Preparar Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Instalar dependencias
        run: npm ci

      - name: Typecheck (¿compila TypeScript?)
        run: npm run typecheck

      - name: Lint (¿código limpio?)
        run: npm run lint

      - name: Pruebas de la API pública (¿sigue siendo segura?)
        run: npm run test:api

      - name: Pruebas de import/export y traspaso de datos
        run: npm run test:csv

      - name: Pruebas de normalización de responsables (¿nombres duplicados?)
        run: npm run test:alias

      - name: Plantilla de la Daily (¿sigue aplicando a Ikigai?)
        run: npm run test:daily

      - name: Mis pendientes (¿el corte de fechas sigue bien?)
        run: npm run test:pendientes

      - name: Periodos del informe (¿los cortes de fecha siguen bien?)
        run: npm run test:periodos

      - name: Limpieza de notas y transcripciones
        run: npm run test:texto

      - name: Build (¿construye la app?)
        run: npm run build
